function max(x, y) {
  return x > y ? x : y;
}

console.log(max(10, 1));
console.log("-----------");

const max2 = (x, y) => (x > y ? x : y);
console.log(max2(10));

//função de dois numeros que retorna o maior deles
