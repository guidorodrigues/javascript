/*Operadores de comparação
    > maior que
    > menor que
    >= maior que ou igual a
    <= menor que ou igual a
    === igualdade restrita (tipo) //checa valor e tipo
    !== difrente restrito (valor tipo) //checa valor e tipo
*/

/*Recomendado não usar
    == igualdade (valor) *************não rerecomendasvel
    != diferente (valor) *********não recomendavel usa
*/

const num1 = 10;
const num2 = 11;
const comp = num1 < num2; // true
console.log(comp); // true
