/*
    Operadores de comparação
    > maior que
    > menor que
    >= maior que ou igual a
    <= menor que ou igual a
    == igualdade (valor) *************não usa
    === igualdade restrita (tipo)
    != diferente (valor) *********não usa
    !== difrente restrito (valor tipo)
*/